Animated side designs demo

How to use

- Open [index.html](index.html) in your browser (double-click or serve via a static server).
- The left and right side designs are animated particles generated by `script.js`.
- To tweak density, edit `LEFT_COUNT` and `RIGHT_COUNT` in `script.js`.
